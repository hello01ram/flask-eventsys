/**
 * This meant to be used as a base file that imports other scripts neccessarry for the application
 * I imported other files (model, view, controller) using ES6 import but introduced a circular dependency bug
 * I spent an hour trying to fix the bug but couldn't so I left this file in case I come back to fixing the error, or,
 * redesigning the application.
 */
